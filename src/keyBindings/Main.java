package keyBindings;

public class Main {
    public static void main(String[] args) {
        new Game();
    }
}
/* KeyBindings = bind an action to KeyStroke
               = don't require you to click a component to give it focus
               = all Swing components use key Bindings
               = increase flexibility compared to key Listeners
               = can assign key Strokes to individual Swing components
               = more difficult to utilise and set up
*/
